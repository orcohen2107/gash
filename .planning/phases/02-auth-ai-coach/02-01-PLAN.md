---
phase: 02-auth-ai-coach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/auth/index.tsx, app/auth/verify.tsx, components/ui/Input.tsx, components/ui/Button.tsx]
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []

must_haves:
  truths:
    - "User can enter Israeli phone number format (+972 50 xxx xxxx or 050 xxx xxxx) on auth screen"
    - "User receives OTP code via SMS after submitting phone number"
    - "User can verify 4-6 digit OTP code on separate verification screen"
    - "On correct OTP, user is authenticated and redirected to main app"
  artifacts:
    - path: "app/auth/index.tsx"
      provides: "Phone input screen with form validation"
      min_lines: 60
    - path: "app/auth/verify.tsx"
      provides: "OTP verification screen"
      min_lines: 60
    - path: "components/ui/Input.tsx"
      provides: "Slab-style RTL input component (Design System)"
      min_lines: 40
    - path: "components/ui/Button.tsx"
      provides: "Primary button component with gradient styling"
      min_lines: 30
  key_links:
    - from: "app/auth/index.tsx"
      to: "@supabase/supabase-js"
      via: "supabase.auth.signInWithOtp({ phone })"
      pattern: "supabase\\.auth\\.signInWithOtp"
    - from: "app/auth/verify.tsx"
      to: "@supabase/supabase-js"
      via: "supabase.auth.verifyOtp({ phone, token })"
      pattern: "supabase\\.auth\\.verifyOtp"
    - from: "components/ui/Input.tsx"
      to: "react-native"
      via: "TextInput component with RTL support"
      pattern: "textAlign.*right|rtl"
---

<objective>
Implement Supabase phone OTP authentication flow. Users can enter Israeli phone numbers, receive SMS OTP codes, verify them, and be authenticated into the app.

Purpose: AUTH-01 (phone sign-up) and AUTH-02 (OTP verification) requirements require working phone input validation and OTP verification screens that integrate with Supabase Auth.

Output: Two auth screens (phone input + OTP verification) with RTL-compliant input components and Design System styling.
</objective>

<execution_context>
@/Users/orcohen/apps/gash/.planning/phases/02-auth-ai-coach/02-CONTEXT.md
@/Users/orcohen/apps/gash/.planning/phases/02-auth-ai-coach/02-RESEARCH.md
@/Users/orcohen/apps/gash/CLAUDE.md
@/Users/orcohen/apps/gash/.planning/DESIGN-SYSTEM.md
</execution_context>

<context>
@/Users/orcohen/apps/gash/.planning/REQUIREMENTS.md
@/Users/orcohen/apps/gash/package.json

**Tech Stack (Phase 1 verified):**
- Supabase Auth with Twilio provider (handles SMS delivery, rate limiting, retry logic)
- react-hook-form v7.72+ for form state and validation
- zod v4.3+ for schema validation
- React Native TextInput with RTL support via I18nManager.forceRTL (set in Phase 1)

**Design System Components:**
- Input: Slab-style (surface-container-high bg, 2px bottom border on focus in primary color)
- Button: Primary gradient (primary #81ecff to primary-dim #00d4ec), 12px radius
- Text: body-lg (1rem Inter, 1.6 line-height), label-md for metadata
- Spacing: 24px vertical between sections, 12px horizontal padding

**Key Constraints from CONTEXT.md:**
- Phone format: Accept +972 prefix or 050 format, auto-prepend +972 if needed
- OTP: 4 or 6 digits (Supabase dashboard configurable, not hardcoded in client)
- Error messages: Hebrew toast notifications (not inline errors)
- No auto-fill from SMS (user enters manually — cleaner security)
- Placeholder: "+972 50 123 4567" (shows format expectation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable Input component (Slab style, RTL)</name>
  <files>components/ui/Input.tsx</files>
  <action>
Create a reusable RTL-compatible input component matching Design System "Slab" style:
- Styled with surface-container-high background (#20201f), 12px padding
- On focus: add 2px bottom border in primary color (#81ecff)
- textAlign: 'right' for RTL (per D-CONTEXT: "never use paddingLeft/Right")
- Props: placeholder, value, onChangeText, secureTextEntry?, keyboardType?, onBlur?, onFocus?
- Placeholder color: on-surface-variant (#adaaaa)
- Text color: white (on-surface)
- No 1px borders (Design System "no-line rule")
- Test RTL rendering with Hebrew placeholder text
  </action>
  <verify>
Component renders correctly: placeholder visible, focus state shows bottom border, RTL text input works. Run: `grep -n "textAlign.*right" components/ui/Input.tsx` (must show right alignment)
  </verify>
  <done>Input.tsx exports a functional component with Slab styling, RTL support, and no directional style props</done>
</task>

<task type="auto">
  <name>Task 2: Create Button component with gradient styling</name>
  <files>components/ui/Button.tsx</files>
  <action>
Create reusable Button component:
- Primary button: Linear gradient from primary (#81ecff) to primary-dim (#00d4ec), 12px border radius
- Props: onPress, title (Hebrew), disabled?, loading?
- Text: bold, on-primary-fixed color
- Touch feedback: opacity reduction on press
- Padding: 12px horizontal, 8px vertical
- No shadow (Design System uses tonal layering, not shadows)
- Default variant: primary (can be extended to secondary/tertiary later)
  </action>
  <verify>
Component renders with gradient background. Run: `grep -n "LinearGradient\|colors:" components/ui/Button.tsx` (confirm gradient colors present)
  </verify>
  <done>Button.tsx exports a functional component with primary gradient, correct colors, and RTL-compatible text</done>
</task>

<task type="auto">
  <name>Task 3: Implement phone input screen (auth/index.tsx)</name>
  <files>app/auth/index.tsx</files>
  <action>
Create phone sign-in screen (AUTH-01 requirement):
- Use react-hook-form + zod for validation
- Phone schema: validates Israeli format (+972 or 050), 10 digits total
- Normalize phone: if starts with 0, replace with +972 prefix before sending to Supabase
- Placeholder: "+972 50 123 4567"
- On submit: call `supabase.auth.signInWithOtp({ phone: normalizedPhone })`
- On error: show toast (use react-native-toast-message) with Hebrew error message
  - Invalid format: "מספר לא חוקי. תן מספר ישראלי." (Invalid number. Provide Israeli number.)
  - Network error: "בעיה בחיבור. בדוק את הרשת." (Connection issue. Check network.)
- On success: navigate to /auth/verify (per Supabase flow)
- Loading state: disable button, show "שליחה..." (Sending...)
- Screen layout: centered, padding 24px, title "כניסה חדשה" (New login), input field, send button

Per D-CONTEXT decision "Phone Input Format": Accept 050 OR +972, auto-prepend +972 if not present
  </action>
  <verify>
Form validates and submits correctly. Run: `grep -n "signInWithOtp\|normalizedPhone" app/auth/index.tsx` (confirm API call)
Manual test: Enter "050 123 4567" → should normalize to "+972501234567" before Supabase call
  </verify>
  <done>Phone input screen renders, validates Israeli phone format, normalizes 050 → +972, calls Supabase auth, shows error toasts on failure</done>
</task>

<task type="auto">
  <name>Task 4: Implement OTP verification screen (auth/verify.tsx)</name>
  <files>app/auth/verify.tsx</files>
  <action>
Create OTP verification screen (AUTH-02 requirement):
- Display user's phone number (from route params or store)
- Input field: 4-6 digits (flexible to Supabase SMS template config)
  - Single masked input field (not separate digit boxes for simplicity)
  - Keyboard type: numeric-pad
  - Auto-focus on render
  - Max length: 6 (flexible if Supabase uses 4-digit OTP)
- On change: auto-submit if all digits entered (no separate submit button needed)
- On submit: call `supabase.auth.verifyOtp({ phone, token: otpCode })`
- On error:
  - Mismatch: show toast "קוד לא נכון. בדוק שוב." (Incorrect code. Check again.)
  - Network: show toast "בעיה בחיבור. בדוק את הרשת." (Connection issue. Check network.)
- On success: navigate to (tabs)/coach (main app)
- Resend link: "לא קיבלת קוד?" (Didn't get code?) → calls `supabase.auth.signInWithOtp({ phone })` again
- Screen layout: title "אימות קוד" (Verify Code), subtitle showing phone, input, resend link

Per D-CONTEXT decision "OTP Verification": No auto-fill from SMS, 4-6 digit flexible, show error toasts
  </action>
  <verify>
OTP input accepts 4-6 digits, submits automatically on full entry. Run: `grep -n "verifyOtp" app/auth/verify.tsx` (confirm API call)
Manual test: Enter valid OTP code from SMS → navigates to main app
  </verify>
  <done>OTP verification screen renders, accepts digits, auto-submits, shows error toasts, resend link works</done>
</task>

</tasks>

<verification>
Manual test sequence:
1. App launches, user is unauthenticated → redirects to /auth (root layout guard)
2. User enters Israeli phone number (try both "050 123 4567" and "+972 50 123 4567" formats)
3. Submit → Supabase sends SMS to Twilio
4. SMS arrives with OTP code
5. User navigates to verify screen
6. User enters OTP code
7. On correct OTP → authenticated, redirected to (tabs)/coach

Error cases:
- Invalid phone format → toast "מספר לא חוקי"
- Network timeout → toast "בעיה בחיבור"
- Wrong OTP → toast "קוד לא נכון"
- Resend OTP → new SMS arrives
</verification>

<success_criteria>
- [ ] Phone input screen renders with Hebrew labels, Slab-style input, primary gradient button
- [ ] Phone validation accepts 050 and +972 formats
- [ ] Phone normalization: "050 123 4567" → "+972501234567"
- [ ] `supabase.auth.signInWithOtp({ phone })` called on submit
- [ ] OTP verification screen renders with numeric input
- [ ] OTP code auto-submits when complete
- [ ] `supabase.auth.verifyOtp({ phone, token })` called on OTP entry
- [ ] Error toasts show for invalid format, network errors, OTP mismatch
- [ ] Resend OTP link triggers new SMS
- [ ] On successful verification, user redirected to main app
- [ ] All text and labels in Hebrew
- [ ] RTL rendering verified (no LTR text, right-aligned inputs)
- [ ] No directional style props (paddingLeft, marginRight, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-ai-coach/02-01-SUMMARY.md` with:
- Authentication flow verified (phone input → OTP → success redirect)
- Supabase Auth integration confirmed (signInWithOtp + verifyOtp working)
- Components created: Input (reusable slab-style), Button (gradient primary), Phone screen, OTP screen
- All error cases handled with Hebrew toasts
- RTL compliance verified on device
</output>
